let arr = [1,2,3,4,5,6];

// console.log(arr);
// console.log(arr.length);

// stack  -LIFO
                        //     Methods
            //.push() :- It pushes the newest element to the last of the array (cause changes to the orignal array). and returns the updated length

    // let x = arr.push(7);
    // console.log(x);
    // let y = arr.push(8);
    // console.log(y);
    
            //.pop() :- Remove the element from the last of the array. it also  cause changes to the array. (It returns the poped out element) 

    // let rem1 = arr.pop();
    // let rem2 = arr.pop();
    // console.log(rem1,rem2); 

            // .shift() :- Remove element form the  start of the  array (Returns the removed value);

    //  let a = arr.shift();
    //  console.log(arr, a);   

            // .unShift() :- Its add the element in the start of the array & it returns the updated array.length;

    // let x = arr.unshift(1.4);
    // console.log(arr , x); 

            // .concat() :- this will not cause any changes in originall array.

    // let arr1 = ['Akash', 'Naveen']
    // let arr2 = arr.concat(arr1);
    // console.log(arr.concat(arr1));
    // console.log(arr);
    // console.log(arr2);

                // .slice() :-
        // let x = arr.slice(1,3);
        // console.log(1,3);

                // .splice() :-



                // Delete 
        // delete arr[arr.length-1]
        // console.log(arr);
        // console.log(arr.length);

        
    

        
